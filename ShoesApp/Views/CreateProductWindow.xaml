<Window x:Class="ShoesApp.Views.CreateProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:ShoesApp.Views"
        mc:Ignorable="d"
        Title="CreateProductWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0">
            <TextBlock Text="Артикул"/>
            <TextBox Text="{Binding NewArticle}"/>
            
            <TextBlock Text="Название"/>
            <TextBox Text="{Binding NewName}"/>
            
            <TextBlock Text="Единица измерения"/>
            <ComboBox
                ItemsSource="{Binding Units}"
                SelectedItem="{Binding SelectedUnit}"
                DisplayMemberPath="Name"
                />
            
            <TextBlock Text="Цена"/>
            <TextBox Text="{Binding NewPrice}"/>
            
            <TextBlock Text="Поставщик"/>
            <ComboBox
                ItemsSource="{Binding Suppliers}"
                SelectedItem="{Binding SelectedSupplier}"
                DisplayMemberPath="Name"
            />
            
            <TextBlock Text="Производитель"/>
            <ComboBox
                ItemsSource="{Binding Manufacturers}"
                SelectedItem="{Binding SelectedManufacturer}"
                DisplayMemberPath="Name"
            />
            
            <TextBlock Text="Категория"/>
            <ComboBox
                ItemsSource="{Binding Categories}"
                SelectedItem="{Binding SelectedCategory}"
                DisplayMemberPath="Name"
            />
            
            <TextBlock Text="Описание"/>
            <TextBox Text="{Binding NewDescription}"/>
            
            <TextBlock Text="Скидка"/>
            <TextBox Text="{Binding NewDiscount}"/>
            
            <TextBlock Text="Количество на складе"/>
            <TextBox Text="{Binding NewQuantity}"/>
            
            <TextBlock Text="Фото"/>
            <TextBox Text="{Binding NewPhoto}"/>
        </StackPanel>
        
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button 
                Content="Сохранить" 
                Command="{Binding CreateProductCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                />
            <Button 
                Content="Отмена" 
                Command="{Binding CancelCommand}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                />
        </StackPanel>
    </Grid>
</Window>
