<Window x:Class="ShoesApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:ShoesApp.Views"
        xmlns:vm="clr-namespace:ShoesApp.ViewModels"
        Icon="/Resources/icon.ico"
        FontFamily="Arial"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Image Grid.Row="0" Width="100" Height="100" Source="/Resources/icon.png"/>
        
        <StackPanel Grid.Row="1">
            <TextBlock Text="Логин"/>
            <TextBox Text="{Binding Login}"/>
            
            
            <TextBlock Text="Пароль"/>
            <TextBox Text="{Binding Password}"/>
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2">
            <Button Content="Войти" Command="{Binding SignInCommand}"/>
            
            <Button Content="Войти как гость" Command="{Binding SignInAsGuestCommand}"/>
        </StackPanel>
    </Grid>
</Window>
